/**
 * Example type definition
 */
export interface Config {
    name: string;
    version: string;
}
/**
 * Example type for options
 */
export interface Options {
    debug?: boolean;
    timeout?: number;
}
